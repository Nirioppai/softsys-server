require("express-async-errors");

class App {
  /**
   * @param {number} port Port of the application
   * @param {{dbHost: string, dbPort: number, dbUser: string, dbPassword: string,
   *  dbName: string}} dbCreds Database credentials
   * @param {{path: string, router: express.Router}[]} routes API endpoints
   */
  
}

module.exports = App;
